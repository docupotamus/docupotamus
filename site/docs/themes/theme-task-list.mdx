---
sidebar_position: 2
---

<!-- import APITable from '@site/src/components/ApiTable'; -->

# 📦 theme-task-list

<!-- Provides the Docs functionality and is the default docs plugin for Docusaurus. -->

## Installation

:::tip
If you use the preset `@docupotamus/docusaurus-preset-classic`, you don't need
to install this as a dependency because it is already included.
:::

```bash npm2yarn
$ npm install --save @docupotamus/docusaurus-theme-task-list
```

## Example Usage

```md title="foo.mdx"
import { TaskList } from '@theme/docupotamus-task-list';

<TaskList>
- [ ] Drink 8 glasses of water.
- [ ] Remember to stretch.
- [ ] Look out at nature.
</TaskList>
```

## Customizing and Configuring

| Name                    | Type                           | Default | Description |
| ----------------------- | ------------------------------ | ------- | ----------- |
| `checkbox.color`        | `React.CSSProperties['color']` | true    | foobar      |
| `checkbox.shape`        | `'circle'` &#124; `'square'`   | true    | foobar      |
| `checkbox.size`         | `'small'` &#124; `'medium'`    | true    | foobar      |
| `progressBar.isEnabled` | `boolean`                      | true    | foobar      |
| `progressBar.color`     | `React.CSSProperties['color']` | true    | foobar      |

### Example Configuration

:::tip
Most users configure this through the `@docupotamus/docusaurus-preset-classic`
preset options.
:::

```js
themeConfig: {
    docupotamusTaskList: {
        checkbox: {
            color: 'red',
            shape: 'circle',
            size: 'small',
        },
    },
},
```
